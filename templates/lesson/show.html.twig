{% extends 'base.html.twig' %}

{% block title %}
{{ lesson.name }}
{% endblock %}

{% block body %}

{# задание: 
1. На странице урока выводим название урока в заголовке, название курса (ссылкой на страницу курса), действия (редактировать, удалить) и контент
2. Доработать, чтобы после создания и удаления урока возвращал на страницу курса
3. Не забываем задавать заголовок страницы и title. В title хорошим тоном считается указывать путь. Например, на странице урока в title выводить “Название урока / Название курса / StudyOn”.

 #}
    <h1>{{ lesson.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lesson.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ lesson.content }}</td>
            </tr>
            <tr>
                <th>serialNumber</th>
                <td>{{ lesson.serialNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lesson_index') }}">back to list</a>

    <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">edit</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
